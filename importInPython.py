import math  # to import whole package
from math import sqrt, pi  # to import some specific fuctions
from math import *  # it will bring up all the functions and variables from math package
import math as sq  # now we can use sq.sqrt() instead of math.sqrt()

from safal import name, standered

name()
print(standered)


print(dir(math))  # show all the function into a specific package

result = math.sqrt(9)
print(result)